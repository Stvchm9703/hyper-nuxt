"use strict";
// import generate from "./generate";
// generate('render:route', (url, result) => {
//   this.$ = cheerio.load(result.html, { decodeEntities: false });
//   //由于window.__nuxt__总是位于body中的第一个script中，
//   //所以我移除了body中第一个脚本标签
//   this.$(`body script`).eq(0).remove();
//   result.html = this.$.html()
// })
